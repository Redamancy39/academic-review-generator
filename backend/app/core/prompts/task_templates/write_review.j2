{# Task template for writing the review - Enhanced version #}
请写一篇中文核心期刊风格的综述稿件，主题为"{{ topic }}"。

{% if user_description %}

**用户的写作期望和要求：**
{{ user_description }}

**重要：请在撰写时充分考虑用户以上述要求，根据用户的具体需求调整：**
- 写作重点和详略分配
- 分析视角和论证深度
- 章节结构和内容组织
- 语言风格和表述方式
{% endif %}

## 硬性要求

### 1. 字数要求（严格执行，不可超出上限！）
- **目标字数：{{ word_count_min }}-{{ word_count_max }} 字（仅统计正文，不含参考文献）**
- **字数上限是硬性约束：绝对不能超过 {{ word_count_max }} 字！**
- 字数过少会导致内容不充实，字数过多会被期刊退稿
- 请在撰写时注意控制各章节篇幅，宁精勿滥
- 如果内容过多，优先保留核心论点和关键数据，删减冗余描述

### 2. 引用要求
- **必须引用所有提供的核心文献**（共 {{ paper_records.split('\n') | length }} 篇）
- 每个论点至少需要 1-2 篇文献支撑
- 引用格式：`[@REFxxx]`，例如 `[@REF001]`、`[@REF002,REF003]`
- **不要遗漏任何一篇核心文献**

### 3. 结构要求
- 按照计划中的章节结构撰写
- 每个章节要有明确的目标和逻辑线索
- 章节之间要有衔接和过渡

### 4. 内容要求
- **禁止逐篇摘要式堆砌**：必须按主题归纳、比较、批判分析
- **每个主要章节都要体现**：
  - 共性：多篇文献的共同发现
  - 差异：不同研究的差异和原因
  - 优缺点：方法或结论的优劣
  - 争议点：存在分歧的问题
- **未来方向**：必须由前文证据推出，不能空泛

### 5. 表格要求（至少 3 个）
- 表1：方法/技术对比表（包含：方法名称、特点、优势、局限、适用场景）
- 表2：性能/结果汇总表（包含：研究对象、数据集、指标、结果、来源）
- 表3：挑战与展望表（包含：挑战、解决方案、研究方向）
- 表格使用 Markdown 格式

### 6. 学术规范
- 引用要准确，不得杜撰数据
- 评价要客观，避免主观臆断
- 用词要规范，符合学术写作要求
- 避免口语化表达

## 综述计划

{{ plan | tojson }}

## 已筛选核心文献（必须全部引用）

{{ paper_records }}

## 证据笔记

{{ evidence_notes | tojson }}

## 跨文献综合分析

{{ synthesis | tojson }}

## 写作检查清单

撰写完成后，请自查以下项目：

1. [ ] 字数是否在 {{ word_count_min }}-{{ word_count_max }} 范围内？
2. [ ] 是否引用了所有核心文献？
3. [ ] 是否包含至少 3 个表格？
4. [ ] 每个章节是否体现了共性、差异、争议？
5. [ ] 未来方向是否有前文证据支撑？
6. [ ] 引用格式是否正确（`[@REFxxx]`）？

## 输出要求

**请直接输出 Markdown 正文，不需要输出 JSON，不需要额外解释。**

从 `# 综述标题` 开始，以 `## 参考文献` 结束（参考文献部分留空，系统会自动填充）。
